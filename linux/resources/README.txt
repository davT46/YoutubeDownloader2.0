Put the three executables needed for the build here (see README.md):

- ffmpeg   (full static build for Linux x86_64, e.g. from https://johnvansickle.com/ffmpeg/)
- ffprobe  (from the same FFmpeg build)
- yt-dlp   (the Linux binary from https://github.com/yt-dlp/yt-dlp/releases)

They are not uploaded to GitHub because they exceed the 100 MB limit.
